self.addEventListener('sync', event => {
    if (event.tag === 'sync-data') {
        event.waitUntil(syncData());
    }
});

async function syncData() {
    // Perform data sync (e.g., fetch from IndexedDB and send to server)
    console.log('Syncing data...');
}
